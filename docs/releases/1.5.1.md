### Changed

- Group panel controls into compact cards with inset dividers and clearer
  selection states.
- Simplify navigation labels and panel text.
- Enlarge panel touch targets and keep controls accessible on narrow screens.

### Fixed

- Retry video after a stream-format change or resize instead of keeping an
  earlier decoder failure locked to JPEG.
- Recover from temporary decoder errors during resizing.
- Restore the common video format when a restrictive viewer pauses or leaves.
- Fix packaging after container builds and stop on errors before replacing
  an existing installer.
